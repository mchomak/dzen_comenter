# Манифест требований

Источник: `2026-09-18-brief.md`. Строку из этого списка может снять только пользователь.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|---|---|---|---|---|
| R01 | «уберем батчи вообще, просто отключим их» | in-ticket | ASSUMPTION — batch-схемы и исторические записи не удаляются, но batch execution path и batch runtime-настройки исключаются из рабочего режима. | T01, T02, T03 |
| R02 | «появился комментарий - сгенерировали ответ - опубликовали» | in-ticket | Один новый комментарий создаёт один generation job; готовый ответ передаётся в существующую durable publication queue в том же цикле. | T03 |
| R03 | «Статусы меняем сразу в БД по ходу дела» | in-ticket | Бизнес-статус комментария и статус технической job фиксируются транзакционно при каждом переходе. | T01, T03 |
| R04 | «при ошибках ретрай через заданное в настройках время» | in-ticket | ASSUMPTION — generation и publication получают отдельные live cooldown и явные лимиты попыток. | T01, T02 |
| R05 | «если ошибка была в генерации ответа, то генерируем заново» | in-ticket | Ошибка generation ставит generation job в retry; следующая claim-операция делает новый AI-вызов. | T01, T03 |
| R06 | «если ответ сгенерировался, а ошибка в публикации, то генерацию по новой не запускаем» | in-ticket | Ошибка publication повторяет только сохранённый reply из publication queue. | T01, T03 |
| R07 | «просто пытаемся через время заново опубликовать» | in-ticket | Publication retry использует live `publication_retry_cooldown_minutes`; готовый текст ответа неизменяем. | T02, T03 |
| R08 | «если ИИ говорит, что комментарий стоит пропустить, то пропускаем без ретрая и даем ему соответсвующий статус» | in-ticket | `SKIP` фиксирует terminal `skipped` для комментария и не создаёт retry/job публикации. | T01, T03 |
| R09 | «статусы должны быть прозрачными и корректными» | in-ticket | Панель показывает техническую причину retry/error и различает generation/publication. | T01, T02, T03 |
| R10 | «сравни инструкции которые мы довали модели сейчас и раньше» | in-ticket | Сравнение Git-версий prompt входит в исследовательский отчёт и приводит к единому строгому output contract. | T04 |
| R11 | «в ответ чаще попадают слова, которых там быть не должно, например "ответ" или "skip" и похожие» | in-ticket | Парсер принимает только machine-readable outcome; display reply не содержит protocol-маркеров. | T03, T04 |
| R12 | «используя наш алгоритм обрезания статей для экономии токенов» | in-ticket | Существующая sanitation/cleanup статьи сохраняется и покрывается регрессией. | T03, T04 |
| R13 | «общий коммит (иследования тоже туда закинь)» | in-ticket | Один итоговый commit включает исходный код, тесты, отчёт сравнения prompt и ранее созданные исследования. | T04 |
| R14 | «деплой на сервере» | in-ticket | После проверок выполняется согласованный production deploy без удаления данных. | T04 |
| R15 | «дождись пока поднимуться контейнеры и проверь чтобы все работало корректно» | in-ticket | Проверка compose health, migration/startup logs, runtime configuration и smoke DB query после deploy. | T04 |
| D01 | *(обнаружено на code review)* publication lease мог завершаться просроченным worker без проверки claim identity | in-ticket | Craft review: `ClaimedPublication` и terminal publication transitions должны требовать claim token; обслуживает R06/R09. | T05 |
