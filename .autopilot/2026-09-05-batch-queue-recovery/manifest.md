# Манифест требований

Источник: `2026-09-05-brief.md`. Строку из этого списка может снять только пользователь.

| ID | Из брифа | Статус | Основание | Где |
|----|----------|--------|-----------|-----|
| R01 | «бот обрабатывал комментарии батчами как это и задумано» | done | Пакет создаётся и обрабатывается по одной статье; для доступных элементов применяется batch-генерация. | code + tests |
| R02 | «избежать ошибок» | done | Недоступный в текущем снимке комментарий не передаётся в публикацию и не прерывает доступные элементы. | code + tests |
| R03 | «[избежать] остановок очереди» | done | Ready-элемент захватывается из БД независимо от DOM и возвращается на ограниченный retry при недоступности. | code + live DB |
| R04 | «добавь фолбеков ретраев и остальных функций для предосторожности» | done | Лимит попыток, cooldown, поэлементный fallback разбора batch и изоляция ошибки публикации. | code + tests |
| R05 | «сделай коммит пуш» | done | `25b0d11` опубликован в `origin/main`. | git |
| R06 | «деплой на сервер после проверь чтобы все работало» | done | Контейнер пересобран; очередь продолжала создавать retry-результаты после деплоя. | server |
